;; (package-install 'powerline)
;; (package-install ')
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
;; (package-install package)
